# Phase 1+2 dependencies.
pygame>=2.5
PyOpenGL>=3.1.6
numpy>=1.24
av>=11.0          # PyAV: ffmpeg bindings for HEVC/H.264 decode
sounddevice>=0.4  # in-process audio output (PortAudio bundled in mac wheel)
